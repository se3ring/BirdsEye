3. Create & Activate a Virtual Environment (recommended)

Open Command Prompt or PowerShell:

cd C:\BirdsEye
python -m venv venv


Activate:

venv\Scripts\activate

Run the Web App

From the terminal (with venv active):

streamlit run app.py


Your browser will open automatically at:

http://localhost:8501